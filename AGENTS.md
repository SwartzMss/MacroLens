# MacroLens 项目约定

## 产品与内容

- MacroLens 面向普通读者，帮助理解宏观经济。概念页先用一句话和生活例子建立直觉，再解释机制、观察方法，最后提供专业定义与证据；按内容需要组织，不强行给每页套完整模板。
- 用具体、自然的中文解释术语。保留统计口径、单位、时间范围、来源和适用条件；区分余额与增速、存量与增量。
- 教学例子明确标注为假设，不伪装成真实数据；缺失数据不编造、不用推算值冒充官方读数。

## 避免偏离范围

- 不引入与当前明确需求无关的新依赖；优先复用现有实现。
- 不为展示效果创建缺少来源或更新方式的正式数据。演示数据应明确标注并与真实数据隔离。
- 不把宏观关系描述成确定因果，不把 AI 生成、缺少可核查证据的宏观结论作为事实发布。
- 新增指标需符合当前需求，具备明确数据来源和解释价值，不以数量作为扩充目标。
- 未经用户明确要求，不扩展为宏观预测、投资评分或买卖建议功能。

## 界面与范围

- 减少重复导航、重复提示和无助于理解的技术标签。图表保留指标名称、单位和可辨认的图例，不能只靠颜色传达区别。
- 卡片合理利用可用宽度，兼顾桌面与手机阅读，避免无意义的留白和过长页面。
- 修改共用组件时检查同类页面的影响，优先在共用实现中修复。用户限定单页试验时遵循该范围；交付时说明实际覆盖了哪些页面。

## 开发与交付

- 新工作默认从最新 `origin/main` 创建 `codex/` 分支，保留已有未提交工作；继续现有 PR 时沿用对应分支。
- 按可独立审阅的主题拆分 PR。用户要求创建 PR 时，完成修改、必要验证、提交和推送后创建 PR，提供链接；合并由用户决定。
- 已授权范围内的编辑、本地验证及修复连续完成，遇到真实阻塞再说明。交付时简要说明结果、验证和未解决问题。

## 按需读取与验证

- 只读取当前任务需要的文件。涉及数据元信息展示时参考 `docs/dataset-metadata-visibility-policy.md`；历史设计与计划按需查阅，并与当前代码核对。
- 使用 `package.json` 要求的 Node.js 版本与 npm 脚本。根据改动选择验证：类型与组件改动用 `npm run check`；页面和内容改动用 `npm run build`，涉及生成页面结构或锚点时接着运行 `npm run test:output`；逻辑与数据处理运行受影响的测试。
- 布局或交互改动检查代表页面的桌面与手机效果。仅修改项目说明文档时检查内容和 diff 即可。
- 必要检查通过后，除非有新改动或未解决的问题，不重复运行无关检查。
